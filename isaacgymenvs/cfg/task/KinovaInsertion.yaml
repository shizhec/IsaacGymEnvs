# used to create the object
name: KinovaInsertion

physics_engine: ${..physics_engine}

debug_vis: False

# if given, will override the device setting in gym. 
env:
  numEnvs: ${resolve_default:8192,${...num_envs}}
  episodeLength: 300
  enableDebugVis: False

  kinovaDofNoise: 0.0

  asset:
    assetRoot: "../../assets/urdf/kinova_description/urdf"
    assetFileNameKinova: "j2n6s300_gripper_clean.urdf"
    factoryAssetRoot: "../../assets/factory/urdf"
    pegAssetFile: "factory_round_peg_16mm_loose.urdf"
    holeAssetFile: "factory_round_hole_16mm.urdf"

sim:
  dt: 0.01667 # 1/60
  substeps: 2
  up_axis: "z"
  use_gpu_pipeline: ${eq:${...pipeline},"gpu"}
  gravity: [0.0, 0.0, -9.81]
  physx:
    num_threads: ${....num_threads}
    solver_type: ${....solver_type}
    use_gpu: ${contains:"cuda",${....sim_device}} # set to False to run on CPU
    num_position_iterations: 8
    num_velocity_iterations: 1
    contact_offset: 0.005
    rest_offset: 0.0
    # bounce_threshold_velocity: 0.2
    max_depenetration_velocity: 1000.0
    default_buffer_size_multiplier: 5.0
    max_gpu_contact_pairs: 1048576 # 1024*1024
    num_subscenes: ${....num_subscenes}
    contact_collection: 0 # 0: CC_NEVER (don't collect contact info), 1: CC_LAST_SUBSTEP (collect only contacts on last substep), 2: CC_ALL_SUBSTEPS (broken - do not use!)